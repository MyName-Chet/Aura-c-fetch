{
  "name": "my-project",
  "version": "1.0.0",
  "description": "A project that retrieves system information and displays it in a formatted manner using the chafa tool.",
  "main": "src/app.ts",
  "scripts": {
    "start": "ts-node src/app.ts",
    "install": "bash install.sh"
  },
  "dependencies": {
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  },
  "devDependencies": {},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "prerequisites": "Install the chafa tool on Ubuntu using the command: sudo apt update && sudo apt install chafa -y."
}